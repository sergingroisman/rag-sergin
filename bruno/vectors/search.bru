meta {
  name: Vector Search (sem LLM)
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/query
  body: json
  auth: none
}

body:json {
  {
    "question": "clean architecture dependency inversion",
    "topK": 5
  }
}

assert {
  res.status: eq 200
  res.body.success: eq true
  res.body.data.results: isArray
}
